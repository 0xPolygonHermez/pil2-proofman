// Must fail to compile: dynamic_range_check_id does not support predefined: 1 (WIP).

require "std_range_check.pil"

airtemplate DynamicRangeCheckIdPredefined(const int N = 2**4) {
    int op = dynamic_range_check_id(0, 2**8-1, predefined: 1);  // ERROR: WIP
}

airgroup DynamicRangeCheckIdPredefined {
    DynamicRangeCheckIdPredefined();
}